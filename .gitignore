# Astro
dist/
node_modules/
.astro/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json

# FTP
.ftpquota

# Strava credentials — upload manuel sur serveur, jamais dans le repo
public/api/strava-env.php
public/api/strava-cache.json

# Endpoint admin unifié — contient le secret, upload manuel sur serveur
public/api/admin-save.php

# Credentials admin — upload manuel sur serveur, jamais dans le repo
public/api/admin-env.php

# Deploy archive
dist-deploy.zip

# Deploy credentials — jamais dans le repo
deploy.env
deploy.log

# Env vars locaux — jamais dans le repo
.env
.env.local
.env.*.local
